{
  "tasks": {
    "dev": "deno task build -i -r && deno run -A --unstable-kv --watch ./bot.gen.ts",
    "build": "deno run -A jsr:@inbestigator/discord-http/cmd build",
    "start": "deno run -A --unstable-kv ./bot.gen.ts"
  },
  "imports": {
    "@inbestigator/discord-http": "jsr:@inbestigator/discord-http@^0.3.10"
  }
}
