{
  "tasks": {
    "dev": "deno task build && deno run -A --unstable-kv --watch ./bot.gen.ts",
    "build": "deno run -A jsr:@inbestigator/discord-http/cmd build -i -r",
    "start": "deno run -A --unstable-kv ./bot.gen.ts"
  },
  "imports": {
    "@inbestigator/discord-http": "jsr:@inbestigator/discord-http@^0.4.2"
  }
}
