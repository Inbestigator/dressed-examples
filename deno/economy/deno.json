{
  "tasks": {
    "dev": "deno task build && deno run -A --watch bot.gen.ts",
    "register": "deno task build -r && deno task start",
    "start": "deno run -A bot.gen.ts",
    "build": "deno run -A jsr:@dressed/cmd build -i"
  },
  "imports": {
    "@db/sqlite": "jsr:@db/sqlite@^0.12.0",
    "@dressed/dressed": "jsr:@dressed/dressed@^1.0.8"
  }
}
