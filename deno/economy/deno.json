{
  "tasks": {
    "dev": "deno task build && deno -A --watch bot.gen.ts",
    "register": "deno task build -r && deno task start",
    "start": "deno -A bot.gen.ts",
    "build": "deno -A jsr:@dressed/cmd build -i"
  },
  "imports": {
    "@db/sqlite": "jsr:@db/sqlite@^0.12.0",
    "@dressed/dressed": "jsr:@dressed/dressed@^1.1.3"
  }
}
