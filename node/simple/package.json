{
  "scripts": {
    "dev": "bun run build -i && bun start",
    "register": "bun run build -r && bun start",
    "build": "dressed build",
    "start": "bun .dressed"
  },
  "devDependencies": {
    "@types/node": "^24.9.1"
  },
  "dependencies": {
    "dressed": "^1.10.0"
  }
}
