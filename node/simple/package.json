{
  "scripts": {
    "dev": "pnpm build && pnpm start",
    "register": "pnpm build -r && pnpm start",
    "build": "dressed build -i",
    "start": "pnpx tsx ./bot.gen.ts"
  },
  "devDependencies": {
    "@types/node": "^22.15.16"
  },
  "dependencies": {
    "dressed": "^1.6.3"
  }
}
