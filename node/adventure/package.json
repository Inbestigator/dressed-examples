{
  "type": "module",
  "scripts": {
    "dev": "bun run build -i && bun start",
    "register": "bun run build -r && bun start",
    "build": "dressed build",
    "start": "bun .dressed"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@dressed/next": "^1.3.0",
    "dressed": "^1.9.1"
  }
}
