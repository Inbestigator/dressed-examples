{
  "name": "adventure-bot",
  "scripts": {
    "build": "dressed build",
    "dev": "bun run build -i && bun start",
    "register": "bun run build -r && bun start",
    "start": "bun .dressed"
  },
  "dependencies": {
    "dressed": "^2.0.0"
  },
  "devDependencies": {
    "@dressed/framework": "^0.6.0",
    "@types/node": "^26.1.1"
  }
}
